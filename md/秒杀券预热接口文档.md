# 秒杀券预热接口文档

## 📋 接口概览

| 接口名称 | 请求方法 | 接口路径 | 功能描述 |
|---------|---------|---------|---------|
| 预热单个秒杀券 | POST | `/admin/seckill/coupon/{id}/preheat` | 预热指定ID的秒杀券到Redis |
| 预热所有秒杀券 | POST | `/admin/seckill/coupon/preheat` | 预热所有有效的秒杀券到Redis |

---

## 🔥 预热单个秒杀券

### 接口信息
- **请求方法**: POST
- **接口路径**: `/admin/seckill/coupon/{id}/preheat`
- **路径参数**: 
  - `id`: 秒杀券ID (Long类型)

### 请求示例
```http
POST /api/admin/seckill/coupon/1/preheat
Content-Type: application/json
```

### 响应格式
```json
{
  "code": 1,
  "msg": "预热成功"
}
```

### 错误响应
```json
{
  "code": 0,
  "msg": "秒杀券不存在"
}
```

---

## 🌟 预热所有秒杀券

### 接口信息
- **请求方法**: POST
- **接口路径**: `/admin/seckill/coupon/preheat`

### 请求示例
```http
POST /api/admin/seckill/coupon/preheat
Content-Type: application/json
```

### 响应格式
```json
{
  "code": 1,
  "msg": "预热所有秒杀券成功"
}
```

### 错误响应
```json
{
  "code": 0,
  "msg": "预热失败"
}
```

---

## 🔧 后端实现建议

### 控制器层实现
```java
@RestController
@RequestMapping("/admin/seckill/coupon")
public class SeckillCouponController {
    
    /**
     * 预热单个秒杀券
     */
    @PostMapping("/{id}/preheat")
    public Result<?> preheatCoupon(@PathVariable Long id) {
        try {
            seckillCouponService.preheatCoupon(id);
            return Result.success("预热成功");
        } catch (Exception e) {
            log.error("预热秒杀券失败，ID：{}", id, e);
            return Result.error("预热失败：" + e.getMessage());
        }
    }
    
    /**
     * 预热所有秒杀券
     */
    @PostMapping("/preheat")
    public Result<?> preheatAllCoupons() {
        try {
            seckillCouponService.preheatAllCoupons();
            return Result.success("预热所有秒杀券成功");
        } catch (Exception e) {
            log.error("预热所有秒杀券失败", e);
            return Result.error("预热失败：" + e.getMessage());
        }
    }
}
```

### 服务层实现
```java
@Service
public class SeckillCouponServiceImpl implements SeckillCouponService {
    
    @Autowired
    private StringRedisTemplate redisTemplate;
    
    @Override
    public void preheatCoupon(Long id) {
        // 1. 查询秒杀券信息
        SeckillCoupon coupon = seckillCouponMapper.selectById(id);
        if (coupon == null) {
            throw new RuntimeException("秒杀券不存在");
        }
        
        // 2. 验证秒杀券状态
        if (coupon.getStatus() != 1) {
            throw new RuntimeException("秒杀券状态无效");
        }
        
        // 3. 预热到Redis
        String stockKey = "seckill:coupon:stock:" + id;
        redisTemplate.opsForValue().set(stockKey, coupon.getStock().toString());
        
        // 4. 设置过期时间（秒杀结束时间）
        long expireSeconds = Duration.between(LocalDateTime.now(), coupon.getEndTime()).getSeconds();
        if (expireSeconds > 0) {
            redisTemplate.expire(stockKey, expireSeconds, TimeUnit.SECONDS);
        }
    }
    
    @Override
    public void preheatAllCoupons() {
        // 1. 查询所有有效的秒杀券
        List<SeckillCoupon> coupons = seckillCouponMapper.selectValidCoupons();
        
        // 2. 批量预热
        for (SeckillCoupon coupon : coupons) {
            try {
                preheatCoupon(coupon.getId());
            } catch (Exception e) {
                log.error("预热秒杀券失败，ID：{}", coupon.getId(), e);
            }
        }
    }
}
```

---

## 🔄 预热业务流程

### 预热单个秒杀券流程
1. 接收预热请求，获取秒杀券ID
2. 验证秒杀券是否存在
3. 验证秒杀券状态是否有效
4. 将库存数据预热到Redis
5. 设置Redis键的过期时间
6. 返回成功或失败消息

### 预热所有秒杀券流程
1. 接收批量预热请求
2. 查询所有有效的秒杀券列表
3. 遍历所有秒杀券，逐个预热
4. 返回成功或失败消息

---

## ⚠️ 注意事项

1. **安全性**：预热操作应该限制权限，只有管理员可以执行
2. **幂等性**：预热操作应该支持重复执行，不会产生副作用
3. **性能**：批量预热时可以考虑使用Redis管道操作
4. **日志**：记录预热操作的详细日志，便于排查问题

---

**文档状态**: ✅ 完整可用  
**更新日期**: 2026-04-20