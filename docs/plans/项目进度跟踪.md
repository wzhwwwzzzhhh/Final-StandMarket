# 末路衣橱 项目进度跟踪

> 基于 [功能拓展主计划.md](功能拓展主计划.md) 各阶段完成情况整理
> 日期: 2026-07-19

---

## 总体状态

| 维度 | 状态 |
|------|------|
| 功能拓展总进度 | ✅ 6/6 阶段全部完成 |
| 代码提交 | ✅ 所有阶段已提交 |
| 本地验证 | ✅ Phase 6 本地验证通过 |
| 生产部署 | ⏳ 待部署（ES 插件已安装） |
| 代码审查 | ⏳ 部分阶段需补审查报告 |

---

## 各阶段完成明细

### Phase 1: 收藏夹

| 项 | 状态 | 说明 |
|---|------|------|
| 数据库表 `favorite` | ✅ | 基础项目自带 |
| 后端 CRUD | ✅ | 项目初始化时已实现 |
| 用户端前端 | ✅ | 收藏按钮 + 收藏列表页 |
| Commit | — | 包含在 `e154bfe 前端+框架` 初始提交中 |

**已知问题**: 无

---

### Phase 2: 商品评价

| 项 | 状态 | 说明 |
|---|------|------|
| 数据库表 `review` | ✅ | 基础项目自带 |
| 后端接口 | ✅ | 用户端 + 管理端完整接口 |
| 用户端前端 | ✅ | 评价表单 + 评价展示 |
| 管理端前端 | ✅ | 评价审核管理页 |
| Commit | — | 包含在 `e154bfe 前端+框架` 初始提交中 |

**已知问题**: 无

---

### Phase 3: 支付网关（支付宝沙箱）

| 项 | 状态 | 说明 |
|---|------|------|
| 支付宝 SDK 依赖 | ✅ | `pom.xml` 已添加 |
| 支付宝配置 | ✅ | `AlipayConfig.java` |
| 支付创建接口 | ✅ | `POST /user/pay/create/{orderId}` |
| 支付回调接口 | ✅ | `POST /notify/paySuccess` |
| 支付状态查询 | ✅ | `GET /user/pay/query/{orderId}` |
| 前端支付对接 | ✅ | 创建订单页调真实支付 |
| Commit | ✅ | `c6c0932 feat: Phase 3 支付宝沙箱支付完整接入` |

**已知问题**: 无

---

### Phase 4: 物流/发货

| 项 | 状态 | 说明 |
|---|------|------|
| `orders` 表加快递字段 | ✅ | `tracking_company` + `tracking_number` |
| 管理端发货接口 | ✅ | `PUT /admin/order/deliver` |
| 用户端物流查询 | ✅ | `GET /user/order/tracking/{orderId}` |
| 管理端发货表单 | ✅ | 订单详情弹窗集成 |
| 用户端物流展示 | ✅ | 订单详情页展示 |
| 订单状态机完整 | ✅ | 待付款 → 待发货 → 待收货 → 已完成 |
| Commit | ✅ | `f9f521f feat: Phase 4 物流/发货功能完整接入` |

**已知问题**: 无

---

### Phase 5: 售后/退款

| 项 | 状态 | 说明 |
|---|------|------|
| 数据库表 `refund` | ✅ | 完整字段 |
| 用户申请退款 | ✅ | `POST /user/refund/apply` |
| 我的退款记录 | ✅ | `GET /user/refund/list` |
| 管理端审核列表 | ✅ | `GET /admin/refund/list` |
| 审核通过（同意退款） | ✅ | `PUT /admin/refund/approve`，自动恢复库存 |
| 审核拒绝 | ✅ | `PUT /admin/refund/reject` |
| 用户端退款页 | ✅ | `RefundList.vue` |
| 管理端审核页 | ✅ | `RefundList.vue` |
| Commit | ✅ | `78a8053 feat: Phase 5 售后退款完整接入` |

**修复的 Bug**:
- `RefundController` admin/user bean 名称冲突（2026-07-19 修复，未提交）

---

### Phase 6: ES 搜索增强

| 项 | 状态 | 说明 |
|---|------|------|
| Docker ES + IK 插件 | ✅ | 服务器 ES 8.11.0 已安装 |
| Docker ES + Pinyin 插件 | ✅ | 服务器 ES 8.11.0 已安装 |
| Kibana | ⏳ | 未部署，暂无紧迫需求 |
| ES 索引 mapping（IK + Pinyin） | ✅ | `products` 索引含 name.pinyin 子字段 |
| 后端数据同步 | ✅ | `rebuildIndex()` 全量 + `@Scheduled` 定时 + CRUD 实时 |
| 手动触发同步 | ✅ | `POST /admin/es/sync` |
| 索引状态查询 | ✅ | `GET /admin/es/status` |
| agent-service 搜索增强 | ✅ | bool 查询 + IK/拼音双通道 + 筛选 + 高亮 + 排序 |
| 管理端同步控制页 | ✅ | `EsSyncControl.vue` |
| Commit | ✅ | `6e423f1 feat: Phase 6 ES 搜索增强` |

**本地验证结果**（2026-07-19）:
- IK 分词: `末路衣橱连衣裙` → `末路/衣橱/连衣裙/衣裙` ✅
- Pinyin 分词: `连衣裙` → `lian/lyq/yi/qun` ✅
- 中文搜索: `连衣裙` → score 8.63 ✅
- 拼音搜索: `lyq` → 匹配到"连衣裙" ✅
- 筛选 + 排序: 正常 ✅
- 数据同步: 74 条商品 MySQL → ES ✅

**修复的 Bug**（2026-07-19，未提交）:
- `ProductIndexServiceImpl.java` — `selectByCondition` 传参修复（空 Map → `{"params": {}}`）
- `ProductSyncTask.java` — 同上修复 + 补充 `Map` import
- `agent-service/.env` — Redis 密码修正（`1234` → `050723`）

---

## 待办事项

| 优先级 | 任务 | 说明 |
|--------|------|------|
| 🔴 高 | **提交未提交的修复** | RefundController bean 命名 + ProductIndexService 传参修复 |
| 🔴 高 | **服务器部署后端** | 需配置 `application-prod.yml` + 服务器 Java 环境 |
| 🟡 中 | **服务器部署前端** | `npm run build` 后部署静态文件 |
| 🟡 中 | **agent-service 部署** | 改 ES_HOST 指向服务器本地 ES |
| 🟢 低 | Kibana 部署 | 可视化 ES 数据，调试用 |
| 🟢 低 | **部署方案文档** | 详细讨论迭代升级方案 |

---

## Git 分支状态

- 当前分支: `feature/review-module`
- 主分支: `main`
- 未提交更改:
  - `RefundController.java`（admin/user）— bean 名称冲突修复
  - `ProductIndexServiceImpl.java` — `selectByCondition` 传参修复
  - `ProductSyncTask.java` — 同上修复
  - 残留文件: `analysis-ik-Latest.zip`, `analysis-pinyin-Latest.zip`（可删除）

---

## 环境信息

| 环境 | 地址 | 说明 |
|------|------|------|
| 开发环境 | Windows 本地 | JDK 17, MySQL, Redis, RabbitMQ |
| 服务器 | Ubuntu 云服务器 | Docker: ES 8.11.0, Redis 7.2, RabbitMQ 3.12 |
| 隧道 | 本地 19200 → 服务器 9200 | ES 通过 SSH 隧道连接 |
