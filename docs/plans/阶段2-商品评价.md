# Phase 2: 商品评价

> 基于主计划 [功能拓展主计划.md](功能拓展主计划.md) Phase 2
> 日期: 2026-07-18

## 步骤

### Step 1: 数据库 - 创建 review 表

SQL 保存到 `mysql/review_table.sql`

```sql
CREATE TABLE `review` (
  `id` bigint NOT NULL AUTO_INCREMENT COMMENT '主键',
  `user_id` bigint NOT NULL COMMENT '用户id',
  `product_id` bigint NOT NULL COMMENT '商品id',
  `order_id` bigint NOT NULL COMMENT '订单id',
  `rating` tinyint NOT NULL COMMENT '评分 1-5',
  `content` varchar(500) DEFAULT NULL COMMENT '评价内容',
  `images` varchar(1000) DEFAULT NULL COMMENT '评价图片(JSON数组)',
  `status` tinyint NOT NULL DEFAULT '1' COMMENT '状态 1显示 0隐藏',
  `create_time` datetime NOT NULL COMMENT '评价时间',
  `update_time` datetime DEFAULT NULL COMMENT '更新时间',
  PRIMARY KEY (`id`),
  KEY `idx_review_product` (`product_id`, `status`, `create_time` DESC),
  KEY `idx_review_user` (`user_id`, `create_time` DESC),
  KEY `idx_review_order` (`order_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb3 COLLATE=utf8mb3_bin COMMENT='商品评价';
```

### Step 2: 后端 - 实体类

`fashion-pojo/src/main/java/com/fashion/entity/Review.java`

字段: id, userId, productId, orderId, rating, content, images, status, createTime, updateTime

### Step 3: 后端 - Mapper 层

- `fashion-server/.../mapper/ReviewMapper.java`
- `fashion-server/.../resources/mapper/ReviewMapper.xml`

方法:
- `insert(Review review)` — 新增评价
- `selectByProductId(@Param("productId") Long productId, @Param("status") Integer status)` — 商品评价列表
- `selectByUserId(@Param("userId") Long userId)` — 我的评价
- `selectByOrderId(@Param("orderId") Long orderId)` — 根据订单查评价（用于校验是否已评）
- `selectById(@Param("id") Long id)` — 详情
- `updateStatus(@Param("id") Long id, @Param("status") Integer status)` — 管理端隐藏/显示
- `selectRatingStats(@Param("productId") Long productId)` — 评分统计（平均分+各评分数量）

### Step 4: 后端 - Service 层

- `fashion-server/.../service/ReviewService.java`
- `fashion-server/.../service/impl/ReviewServiceImpl.java`

方法:
- `addReview(Review review)` — 添加评价（校验订单归属、是否已评）
- `getProductReviews(Long productId, Integer page, Integer size, Integer rating)` — 商品评价列表（分页+评分筛选）
- `getMyReviews(Long userId, Integer page, Integer size)` — 我的评价
- `getReviewStats(Long productId)` — 评分统计
- `updateReviewStatus(Long id, Integer status)` — 管理端控制显示/隐藏

### Step 5: 后端 - Controller

#### 用户端 `ReviewController.java`

| 接口 | 方法 | 说明 |
|------|------|------|
| `POST /user/review/add` | 添加评价 | 含图片上传（复用 OSS） |
| `GET /user/review/list/{productId}` | 商品评价列表 | 分页，支持评分筛选 |
| `GET /user/review/stats/{productId}` | 评分统计 | 返回平均分+各评分数量 |
| `GET /user/review/my` | 我的评价列表 | 分页 |

#### 管理端 `ReviewController.java`

| 接口 | 方法 | 说明 |
|------|------|------|
| `GET /admin/review/list` | 评价列表 | 全量，分页，支持商品名搜索 |
| `PUT /admin/review/status` | 隐藏/显示 | 修改 status |
| `DELETE /admin/review/{id}` | 删除评价 | 慎用，软删除优先 |

### Step 6: 前端 - 用户端

#### 6a. API 封装

`frontend/fashion-client/src/api/review.js`

方法:
- `addReview(data)` — POST
- `getProductReviews(productId, params)` — GET
- `getReviewStats(productId)` — GET
- `getMyReviews(params)` — GET

#### 6b. ProductDetail.vue — 评价区域改为真实 API

当前状态: 有硬编码的 mock 评价数据（data 中 `reviews` 数组）。

改动:
1. 移除 data 中的 `reviews` 硬编码数组
2. 移除 `averageRating`、`filteredReviews` 两个 computed（改为从 API 获取）
3. 保留页面上的评价区域结构（`<div class="product-reviews">`），但改为 API 驱动
4. `created()` 中新增调用 `loadReviews()` 和 `loadReviewStats()`
5. 评价筛选改为请求 API 传参

#### 6c. Order.vue — 增加"去评价"入口

- 订单状态 `status === 4`（已完成）的订单项中，增加"去评价"按钮
- 点击跳转到 AddReview 页面或弹窗

#### 6d. AddReview.vue — 新增评价表单页

`frontend/fashion-client/src/views/AddReview.vue`

功能:
- 评分星星组件（Element Plus el-rate）
- 评价文字输入
- 图片上传（复用 OSS 上传接口）
- 提交评价

路由: `/add-review/:orderId/:productId`

### Step 7: 前端 - 管理端

#### 7a. API 封装

`frontend/fashion-admin/src/api/review.js`

#### 7b. ReviewList.vue — 管理端评价管理

`frontend/fashion-admin/src/views/ReviewList.vue`

功能:
- 评价列表（分页）
- 按商品名搜索
- 隐藏/显示操作
- 查看评价详情

路由: 在管理端路由配置中新增 `/admin/review`

### Step 8: 审查 + 前端检查

1. 使用 code-reviewer 审查后端代码
2. 前端检查：ProductDetail.vue 评价区域是否正常、有无残留 mock 数据
3. 检查 Order.vue 各状态按钮布局是否合理

---

## 文件变更清单

### 新增文件

| 文件 | 分类 |
|------|------|
| `mysql/review_table.sql` | 数据库 |
| `fashion-pojo/.../entity/Review.java` | 后端 |
| `fashion-server/.../mapper/ReviewMapper.java` | 后端 |
| `fashion-server/.../resources/mapper/ReviewMapper.xml` | 后端 |
| `fashion-server/.../service/ReviewService.java` | 后端 |
| `fashion-server/.../service/impl/ReviewServiceImpl.java` | 后端 |
| `fashion-server/.../controller/user/ReviewController.java` | 后端 |
| `fashion-server/.../controller/admin/ReviewController.java` | 后端 |
| `frontend/fashion-client/src/api/review.js` | 前端 |
| `frontend/fashion-client/src/views/AddReview.vue` | 前端 |
| `frontend/fashion-admin/src/api/review.js` | 前端 |
| `frontend/fashion-admin/src/views/ReviewList.vue` | 前端 |

### 修改文件

| 文件 | 改动 |
|------|------|
| `frontend/fashion-client/src/views/ProductDetail.vue` | mock 评价 → 真实 API |
| `frontend/fashion-client/src/views/Order.vue` | 增加"去评价"按钮 |
| `frontend/fashion-client/src/router/index.js` | 新增 AddReview 路由 |
| `frontend/fashion-admin/src/router/index.js` | 新增 ReviewList 路由 |

---

## 审查报告

> 审查时间: 2026-07-18
> 审查范围: 全部新增和修改文件
> 审查方式: subagent code-reviewer

### 发现问题及修复

| # | 问题 | 严重度 | 状态 |
|---|------|--------|------|
| 1 | Review 实体缺少 `productName` 字段，导致管理端商品名显示空白 | 🔴 严重 | 已修复 |
| 2 | AddReview.vue 引用不存在的 `@/utils/auth`，加载会报错 | 🔴 严重 | 已修复 |
| 3 | 评分统计 SQL 返回 `avg_rating` 但前端读 `avgRating`，始终显示 0 | 🔴 严重 | 已修复 |
| 4 | `JSON.parse('')` 在空字符串时抛异常，破坏整页渲染 | 🟡 重要 | 已修复 |
| 5 | `parseInt(undefined)` 可能产生 NaN 传给后端 | 🟡 重要 | 已修复 |
| 6 | AdminReviewController 未判空，缺参时 NPE | 🟡 重要 | 已修复 |
| 7 | 缺少 rating 1-5 范围和 content 500字限制校验 | 🟢 建议 | 已修复 |
| 8 | ProductDetail.vue 的 `relatedProducts` 仍为硬编码 mock 数据 | ⚪️ 遗留 | 已有代码，后续优化 |
| 9 | SQL 表使用废弃的 `utf8mb3` | ⚪️ 保留 | 与项目现有表保持一致 |

### 验证通过项

- 后端编译通过
- 所有 API 路径与前端调用一致
- Controller 遵循 `/user/review`、`/admin/review` 前缀规范
- JWT 鉴权正确使用 `BaseContext.getUserId()`
- 用户端和管理端路由配置正确
