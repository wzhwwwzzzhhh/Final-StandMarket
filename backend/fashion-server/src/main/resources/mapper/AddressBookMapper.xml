<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fashion.mapper.AddressBookMapper">

    <!-- 插入地址 -->
    <insert id="insert" parameterType="com.fashion.entity.AddressBook">
        insert into address_book (
            user_id, consignee, sex, phone, province_code, province_name, city_code, city_name, 
            district_code, district_name, detail, label, is_default
        ) values (
            #{userId}, #{consignee}, #{sex}, #{phone}, #{provinceCode}, #{provinceName}, #{cityCode}, #{cityName},
            #{districtCode}, #{districtName}, #{detail}, #{label}, #{isDefault}
        )
    </insert>

    <!-- 根据ID删除地址 -->
    <delete id="deleteById" parameterType="long">
        delete from address_book where id = #{id}
    </delete>

    <!-- 更新地址 -->
    <update id="update" parameterType="com.fashion.entity.AddressBook">
        update address_book
        <set>
            <if test="consignee != null">consignee = #{consignee},</if>
            <if test="sex != null">sex = #{sex},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="provinceCode != null">province_code = #{provinceCode},</if>
            <if test="provinceName != null">province_name = #{provinceName},</if>
            <if test="cityCode != null">city_code = #{cityCode},</if>
            <if test="cityName != null">city_name = #{cityName},</if>
            <if test="districtCode != null">district_code = #{districtCode},</if>
            <if test="districtName != null">district_name = #{districtName},</if>
            <if test="detail != null">detail = #{detail},</if>
            <if test="label != null">label = #{label},</if>
            <if test="isDefault != null">is_default = #{isDefault},</if>
        </set>
        where id = #{id}
    </update>

    <!-- 根据ID查询地址 -->
    <select id="getById" parameterType="long" resultType="com.fashion.entity.AddressBook">
        select * from address_book where id = #{id}
    </select>

    <!-- 查询用户的地址列表 -->
    <select id="listByUserId" parameterType="long" resultType="com.fashion.entity.AddressBook">
        select * from address_book where user_id = #{userId} order by is_default desc, id desc
    </select>

    <!-- 查询用户的默认地址 -->
    <select id="getDefaultByUserId" parameterType="long" resultType="com.fashion.entity.AddressBook">
        select * from address_book where user_id = #{userId} and is_default = 1
    </select>

    <!-- 将用户的所有地址设置为非默认 -->
    <update id="resetDefaultByUserId" parameterType="long">
        update address_book set is_default = 0 where user_id = #{userId}
    </update>

</mapper>