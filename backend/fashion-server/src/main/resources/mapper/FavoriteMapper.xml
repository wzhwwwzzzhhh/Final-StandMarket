<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fashion.mapper.FavoriteMapper">
    <insert id="insert" parameterType="com.fashion.entity.Favorite" useGeneratedKeys="true" keyProperty="id">
        insert into favorite(user_id, product_id, create_time)
        values(#{userId}, #{productId}, #{createTime})
    </insert>

    <delete id="deleteByUserIdAndProductId">
        delete from favorite
        where user_id = #{userId} and product_id = #{productId}
    </delete>

    <select id="selectByUserIdAndProductId" resultType="com.fashion.entity.Favorite">
        select * from favorite
        where user_id = #{userId} and product_id = #{productId}
        limit 1
    </select>

    <select id="selectByUserId" resultType="com.fashion.entity.Favorite">
        select * from favorite
        where user_id = #{userId}
        order by create_time desc
    </select>

    <select id="countByUserId" resultType="int">
        select count(*) from favorite
        where user_id = #{userId}
    </select>

    <delete id="deleteByProductId">
        delete from favorite where product_id = #{productId}
    </delete>
</mapper>
