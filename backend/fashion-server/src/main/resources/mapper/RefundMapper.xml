<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fashion.mapper.RefundMapper">

    <!-- 插入退款记录 -->
    <insert id="insert" parameterType="com.fashion.entity.Refund" useGeneratedKeys="true" keyProperty="id">
        insert into refund (
            order_id, order_detail_id, user_id, refund_no, reason, amount,
            status, order_status, audit_opinion, audit_time, refund_time,
            create_time, update_time
        ) values (
            #{orderId}, #{orderDetailId}, #{userId}, #{refundNo}, #{reason}, #{amount},
            #{status}, #{orderStatus}, #{auditOpinion}, #{auditTime}, #{refundTime},
            #{createTime}, #{updateTime}
        )
    </insert>

    <!-- 根据ID查询 -->
    <select id="getById" parameterType="long" resultType="com.fashion.entity.Refund">
        select * from refund where id = #{id}
    </select>

    <!-- 根据退款单号查询 -->
    <select id="getByRefundNo" parameterType="string" resultType="com.fashion.entity.Refund">
        select * from refund where refund_no = #{refundNo}
    </select>

    <!-- 用户退款列表 -->
    <select id="listByUserId" parameterType="long" resultType="com.fashion.entity.Refund">
        select * from refund
        where user_id = #{userId}
        order by create_time desc
    </select>

    <!-- 管理端全部退款列表 -->
    <select id="listAll" resultType="com.fashion.entity.Refund">
        select * from refund
        <where>
            <if test="status != null">
                and status = #{status}
            </if>
        </where>
        order by create_time desc
    </select>

    <!-- 根据订单ID和状态查询 -->
    <select id="listByOrderIdAndStatus" resultType="com.fashion.entity.Refund">
        select * from refund where order_id = #{orderId} and status = #{status}
    </select>

    <!-- 更新退款记录 -->
    <update id="update" parameterType="com.fashion.entity.Refund">
        update refund
        <set>
            <if test="status != null">status = #{status},</if>
            <if test="auditOpinion != null">audit_opinion = #{auditOpinion},</if>
            <if test="auditTime != null">audit_time = #{auditTime},</if>
            <if test="refundTime != null">refund_time = #{refundTime},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
        </set>
        where id = #{id}
    </update>

</mapper>
