<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fashion.mapper.OrderDetailMapper">

    <!-- 根据订单ID查询订单明细 -->
    <select id="listByOrderId" parameterType="long" resultType="com.fashion.entity.OrderDetail">
        select * from order_detail where order_id = #{orderId}
    </select>

    <!-- 批量插入订单明细 -->
    <insert id="batchInsert" parameterType="java.util.List">
        insert into order_detail (
            name, image, order_id, product_id, combination_id, sku_info, number, amount
        ) values
        <foreach collection="orderDetails" item="item" separator=",">
            (
                #{item.name}, #{item.image}, #{item.orderId}, #{item.productId}, 
                #{item.combinationId}, #{item.skuInfo}, #{item.number}, #{item.amount}
            )
        </foreach>
    </insert>

</mapper>
