接口保证：接口的参数和返回值都必须符合RESTful风格，参数必须使用路径参数，返回值必须使用JSON格式。
数据库保证：查询数据库结构D:\market-handsome\Final-StandMarket\mysql中查询
使用完8080端口后，必须关闭连接，释放资源。
每次任务记得分点，方便调试。
每次生成md文档记得放在D:\market-handsome\Final-StandMarket\md目录下。